enum Services {
    code = 'We help write scalable code for your organization',
    design = 'We help design the best user interfaces for your websites',
    model = 'We help train the best model for cv tasks'
}

let model = Services.model

console.log(model)